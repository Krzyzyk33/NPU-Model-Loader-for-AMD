# NPU-Model-Loader-for-AMD
NPU ONNX Runner is a lightweight, text-only local AI client designed exclusively to load and run LLMs in the ONNX format. By utilizing 100% of your NPU, it leaves the CPU and GPU completely untouched. Features include a minimal text interface and a local API server, ensuring maximum battery savings and silent performance.
